# NuQu — Project Context

Deep context for the NuQu project. Loaded on demand (not every conversation). For standing rules and commands, see `CLAUDE.md`.

## The reference paper
- Watson, Bringewatt, Shaw, Childs, Gorshkov, Davoudi, *"Quantum Algorithms for Simulating Nuclear Effective Field Theories"* — arXiv:2312.05344 (2023/2025). Local PDF at `claude/Watson2025.pdf`.
- Watson et al. estimate fault-tolerant resources for several nuclear EFTs (pionless, one-pion-exchange / static-pion, dynamical-pion) using **Trotterization** for `e^{iHt}`. Pionless is cheapest; **dynamical pion is by far the most expensive**, and that is the regime this project targets.
- Specific results from Watson reused here:
  - **Lemma 5** → field-cutoff and qubit-count formulas (Eqs. 75–78) for the bosonic pion encoding: `pi_max`, `Pi_max`, `n_b = ceil(log2(...))`. Implemented in `src_PI/hamiltonians/core/EFTParameters.py::calculate_dynamic_cutoffs`.
  - **Lemma 23** → Trotter step T-cost: `g(L,n_b) * (1.15 log10(2 g/δ) + 9.2)` with `g = (45 n_b² + 114 n_b + 76) L³`. Implemented in `src_PI/trotter_theory/trotter_theory.py`.
  - **Lemma 78** → WT–WT commutator bound (used for nested-commutator Trotter error estimates).
- Physical inputs (Table I/IV of Watson) are encoded in `EFTParameters.get_physical_parameters` — `M_N=938`, `m_pi=135`, `f_pi=93`, `g_A=1.26`, `a_L=2.2 fm`, contact terms `C=-51.94`, `C_I=1.73` MeV.

## What this project does differently
- Replaces Trotterization with **Qubitization** (block-encoded walk operator + QPE) using `pyLIQTR`. Initial finding: Qubitization gives **lower T-counts** than Trotterization at the same accuracy — that's the headline result so far and the motivation for the publication push.
- The Hamiltonian is **split-oracle**: position-basis terms (`H_pos`: free pion mass + gradient + axial-vector + Weinberg–Tomozawa + static nucleon hopping/contacts) and momentum-basis terms (`H_mom`: pion conjugate-momentum kinetic) are encoded as separate walks. They run sequentially on the same hardware, so the peak logical-qubit count is `max(pos_walk_qubits, mom_walk_qubits)` (T/Clifford gate counts, by contrast, are summed across the two walks). Each walk step also requires a QFT/IQFT pair to swap bases — counted in `calculate_qft_cost`.
- Qubits per site: `4 + 3*n_b` (4 nucleon modes from spin × isospin, plus 3 pion species × `n_b` bits each). Total qubits: `L^dim * (4 + 3 n_b)`.

## Key code map
- `run_nucleon_sweep.py` — top-level sweep over nucleon number `A` (dense 1–10, sparse 20–100) at fixed `L` (default 2; "crank to 10 for paper-grade HPC"). Calls `evaluate_resources` and dumps JSON via `save_sweep_data`.
- `plot_sweep_data.py` — loaders + plotters: single-L T-counts/Λ/runtime, qubitization-vs-Trotter total cost vs A, total cost vs L for chosen A. All plots saved under `data/<date>/`.
- `src_PI/hamiltonians/`
  - `ConstructEFT.py` — assembles `H_pos` and `H_mom` (Jordan–Wigner on the static fermion sector).
  - `core/DynamicalPion.py` — `H_pion_free`, `H_axial_vector` (H_AV), `H_WT_Logic` (Weinberg–Tomozawa).
  - `core/StaticTerms.py` — nucleon hopping + on-site + contact (`HC`, `HCI2`).
  - `core/Operators.py` — `Pi_Squared`, `Gradient_Squared`, `Momentum_Squared`, `Nucleon_Transition_JW`.
  - `core/EFTParameters.py` — physical constants, dynamic cutoffs, `T_cross` time scale.
  - `Lattice1D/` — older 1D-only implementations; treat as legacy reference.
- `src_PI/estimation/`
  - `EstimateResources.py` — orchestrator: build → normalize → pyLIQTR analysis → QFT overhead → package metrics.
  - `NormalizeHamiltonians.py` — strips identity terms (tracked as classical shift), divides by `Δ = safety * Λ` (default `safety_factor=2.5`) so eigenvalues fit in [0, 0.5] for QPE.
  - `estimators.py` — `run_qubitization_analysis`: builds two `PauliLCU` block encodings, two `QubitizedWalkOperator`s, calls `pyLIQTR.utils.resource_analysis.estimate_resources` on each, and sums.
  - `instances.py` — `MyCustomHamiltonian` adapter from OpenFermion `QubitOperator` to pyLIQTR `ProblemInstance`.
- `src_PI/trotter_theory/` — Watson Trotter-cost formulas for the comparison curves in the plots.
- `src_PI/utils/`
  - `LatticeGeometry.py` — flat-index ↔ coordinates, OBC neighbors, qubit index mapping (`site_to_nucleon_qubit`, `site_to_pion_qubit`).
  - `utils.py` — Pauli matrices, chiral coefficients, `P/Q` and `P'/Q'` encoding params (Watson Eq 29/32–33).
  - `DataIO.py` — JSON dump to `data/<YYYY-MM-DD>/sweep_L{L}_{dim}D_<HHMMSS>.json`.

## Conventions
- Boundary conditions: **Open** (OBC). `get_neighbors` and the gradient terms enforce `coords[d] < L-1` for forward derivatives.
- Energy unit throughout: **MeV**. Length conversions via `hc = 197.327 MeV·fm`.
- Data files are date-stamped under `data/`; raw outputs are gitignored (only `data/.gitkeep` is tracked).

## Bosonic cutoff parameters: π_max, Π_max, δπ, n_b
The Watson bosonic encoding has **two physically meaningful parameters and two derived ones**, despite four numbers appearing in the code (`pi_max`, `Pi_max`, `delta_pi`, `n_b`). Knowing which are independent and which are derived matters when choosing or auditing cutoffs.

### Structural relations (Watson §II.C.2.a, Eqs. 28–33)
- Eq. 28: `n_b = log₂(2·π_max/δπ + 1)` → grid relation: `δπ = 2·π_max/(2^{n_b} − 1)`
- Eq. 32: `Π_max = π / (a_L³ · δπ)` → FFT/Nyquist relation: `Π_max ∝ 1/δπ`
- Combined (Eq. 78): `n_b = log₂(2·a_L³·π_max·Π_max/π + 1)`

So once you pick `π_max` and `Π_max`, both `δπ` and `n_b` are determined. **The two free parameters are (π_max, Π_max)**, not (π_max, δπ, n_b).

### What Lemma 5 actually fixes
Lemma 5 gives **two independent lower bounds** (one per parameter), derived from Watson Appendix D's Chebyshev-style energy-bound argument:
- Eq. 75: `π_max ≥ π_max_bound` (controls field-amplitude aliasing for states with `⟨H⟩ ≤ E_bound`)
- Eq. 76: `Π_max ≥ Π_max_bound` (controls conjugate-field aliasing)

`δπ` does not get a separate error bound in App. D — once `(π_max, Π_max)` are chosen, the FFT is exact and `δπ` is determined.

### The integer-ceiling slack and how to use it
Watson p. 26 explicitly notes: "since `n_b` is the number of qubits, it must be an integer. In practice we do not exactly substitute the bounds for `π_max` and `Π_max` into Eq. (78). Rather, we choose the nearest cutoffs above these bounds to ensure `n_b` is an integer."

Current code (`EFTParameters.calculate_dynamic_cutoffs`) returns `pi_max = π_max_bound` and `Pi_max = Π_max_bound` exactly, then `n_b = ceil(log₂(...))`. Because `n_b` is rounded up, the *effective* `Π_max` used downstream — computed from `δπ = 2·π_max/(2^{n_b}−1)` and `Π_max = π/(a_L³·δπ)` — is generally **larger than the Eq. 76 bound**. The slack is silently absorbed into `Π_max`.

### Why this matters for Λ
Λ has contributions roughly `Λ ≈ c_π · π_max² + c_Π · Π_max² + (small cross terms)`. Under the constraint `π_max · Π_max = const` (set by the chosen integer `n_b`), the Lagrangian optimum is `c_π · π_max² = c_Π · Π_max²` (the two contributions equal).

The Λ audit at L=2, dim=3, A=2 measured **c_Π · Π_max² = 73.25%** of Λ and `c_π · π_max² = 26.69%`. The current implicit allocation (slack absorbed into `Π_max`) makes Π² *larger*, going the **wrong direction**. The optimum at this parameter point is `π_max → 1.29·π_max_bound`, `Π_max → 0.78·Π_max_effective` (still above the Eq. 76 bound), giving roughly **~12% Λ reduction** for free.

(The 73/27 ratio drifts with (A, L) since `π_max²`'s T2/T3 pieces grow like `A`; the optimum shifts but the *principle* — re-allocate slack to equalize contributions — is constant.)

### Bottom line for code authors
- `(π_max, Π_max)` are the meaningful dials. `δπ` and `n_b` are derived.
- Macridin/Klco-Savage's NS prescription replaces Lemma 5's energy-bound `(π_max_bound, Π_max_bound)` with NS-optimal bounds tied to a boson-number cutoff. See `claude/research/bosonic-encodings/01_two_readings_oscillator_basis.md` for the three-reading taxonomy (B = NS-optimal cutoff in field-amplitude register, A = occupation-number register, C = basis-adapted compression on top of B via displaced HO or variational basis à la Li 2023). **Reading B is now implemented** as `calculate_ns_cutoffs` (selected via `config.cutoff_method='ns'`); see Live state.
- The slack-reallocation Λ reduction is independent of those three readings and available *now* — it's a tweak inside `calculate_dynamic_cutoffs`, not a new derivation.

## Known issues / TODOs the user has flagged
- `utils.py` has a TODO block about generalizing strides/PBC, but the dimension-agnostic helpers in `LatticeGeometry.py` already cover most of it; the `_1D` helpers in `utils.py` are legacy duplicates.

## Resolved
- *Logical-qubit count not saving properly* (fixed): two bugs — `EstimateResources.py` was reading the pyLIQTR key as `'Logicalqubits'` while pyLIQTR returns `'LogicalQubits'`; and the `combined // 2` halving was wrong. `estimators.run_qubitization_analysis` now returns `LogicalQubits = max(pos, mom)` plus per-walk diagnostics (`Pos_LogicalQubits`, `Mom_LogicalQubits`); `evaluate_resources` exposes `Logical_Qubits`, `Pos_Walk_Logical_Qubits`, `Mom_Walk_Logical_Qubits`; `run_nucleon_sweep.py` saves all three. Verified by `tests/test_logical_qubits.py` (L=2, dim=2, A=1: pos=245, mom=244, reported=245).

## Live state
- Venv: Python 3.10.11, `pip install -r requirements.txt` clean. `pypdf` installed in the venv for Claude's use (read `claude/Watson2025.pdf` via `from pypdf import PdfReader` — 120 pages).
- `tests/` directory exists with `test_logical_qubits.py`. Run from project root: `python -m tests.test_logical_qubits`.
- `claude/research/VC Encoding/` exists with 9 PDFs and a synthesis `00_literature_review.md` (Verstraete–Cirac and successors). Headline conclusions: VC primarily helps the static-nucleon hopping operator (currently O(L) JW string under the existing implementation); 3D VC adds ≈3× qubits but gives O(1) Pauli weight for nearest-neighbor terms. A useful next deliverable would be a JW-vs-VC comparison run through the existing pyLIQTR pipeline.
- `claude/research/lambda audit/` exists with `00_lambda_analysis.md`. Headline: the LCU Λ values (10⁹–10¹³ MeV) are NOT a bug — they're driven >99.9% by the bosonic free-pion sector (`m_π² π²` and `Π²`), where Λ ~ N_sites × pi_max². The fermion sector is ~ppm of total. Switching units doesn't help (Λ/ΔE is dimensionless). **`E_bound` is *not* a meaningful dial** (the contact and chiral-coupling pieces of `pi_max²` dwarf any reasonable `E_bound` choice — < 1% impact). The only cheap Λ-reduction lever is loosening `epsilon_cut` (currently 0.1; Λ ∝ 1/eps). Bigger structural win would be a Galerkin / oscillator pion encoding (replaces Λ ~ cutoff² with Λ ~ ω · N_basis). Quantum-chemistry's small Λ comes from absence of bosonic fields and from low-rank factorizations (DF/THC) that don't transplant directly to digital binary boson encodings.
- **Energy convention in the EFT (clarified on follow-up):** the EFT Hamiltonian is non-relativistic and rest-mass-stripped, so `⟨H⟩ = 0` corresponds to A free nucleons at zero momentum with no pion excitations. Ground-state eigenvalues are therefore `≈ −BE` for bound nuclei (modulo EFT-truncation and lattice-spacing error). The `E_bound` parameter in Lemma 5 is an upper bound on `⟨ψ|H|ψ⟩` for states the cutoff must represent, *not* a value of any eigenvalue — these are different energies that share the word "bound."
- **Path B / Reading B (NS-optimal amplitude cutoff) implemented.** New `cutoff_method` axis on `Config` ('energy_bound' default = Watson Lemma 5; 'ns' = Nyquist-Shannon). `EFTParameters.calculate_ns_cutoffs` sets `pi_max=√(π·2^n_b/(2·a_L^d·ω_0))`, `Pi_max=√(π·2^n_b·ω_0/(2·a_L^d))` (Macridin Eq. 87, field-theory units; ω_0=m_0=m_π), with `N_b=2^n_q` from the shared `estimate_boson_cutoff` (renamed from `calculate_fock_cutoff`) and `n_b=⌈log₂(2N_b)⌉=n_q+1`. Same amplitude register + operators + split-oracle/QFT as energy_bound — only the windows change. `get_Pp_Qp` now takes `dim` (default 3) and uses `a_L**dim`; the matching ratio `Pi_max/pi_max=ω_0` falls out of the existing conjugate grid automatically (realized `Pi_max` = NS `Pi_max`·(2^n_b−1)/2^n_b, one grid cell). Verified by `tests/test_ns_cutoffs.py`. **A/B at L=2,d=3,A=1:** energy_bound (n_b=19, 502 logical qubits, Λ=3.96e9) vs NS (n_b=6, 189 logical qubits, Λ=6.47e5) — ~2.7× qubits, **~6100× Λ** (the projected "~10×" was the naive (20/6)²; at low A nothing floors the bosonic collapse, so the realized cut is far larger — expect it to shrink at high A as the contact floor (∝A) and the heuristic's A-growth of N_b kick in). Plotting (`plot_sweep_data.py`) now keys color/label on a basis+cutoff "series" so amplitude/energy_bound vs amplitude/ns vs fock are distinguishable.
- **Lambda audit v2 (2026-06-10):** `claude/research/lambda audit/01_lambda_audit_v2_basis_encoder.md` + reproducible `checks_v2.py`. Compares the three live paths at L=2,d=3 (A∈{1,2,10}). Headline: NS-amplitude and Fock/sparse each cut **Λ by ~6,600–7,200×** vs energy_bound (A=2: 8.35e9 → 1.25e6 NS / 1.16e6 Fock), and since `N_walk = √2π·Λ/ΔE`, that's the same factor off the QPE step count → QPE-total-T drops ~9.7e16 → ~1.1–1.5e13. **Per-walk-step T is comparable across all three (~2–5e6 T)** — the entire win is in Λ/N_walk, not a cheaper walk (Fock/sparse is even slightly pricier per step). Where Λ lives *flips*: free-pion bosonic collapses, so the chiral sector becomes visible — NS: H_AV 4–11%; Fock/sparse: mixed H_AV+H_WT 34–37% (was 0.06% in v1). Where Fock T/walk lives: ~58% boson-monomial SELECT (free-pion gradient²) + ~41% mixed-chiral Gilyén products + <1% fermion; atom = single-mode (â+â†) BCK, cost grows with n_b. **L⁶ wall gone:** at fixed n_b, Λ ~ #sites ~ L³ (Fock L=2→8 sweep: 64× sites → 89× Λ). Caveat: sparse T is an analytical upper bound (boson) / lower bound (fermion <1%); n_q is still the generous heuristic (Tong bound = open homework), inherited by both new paths so the comparison stays fair.
- **Tong boson-cutoff wired + PT2 C++ port (2026-07-08).** Two classical-pipeline additions:
  - `estimate_boson_cutoff` gains a `boson_cutoff_method` axis on `Config` ('heuristic' default = the log2(1+A) starter formula; 'tong' = rigorous Tong-2022 bound via `classical/trimci/tong_bound.py::cutoff_predictions`, returning the certified `max(n_b_eng, n_b_spec1)`). At L=2/3, d=3 the 'tong' cutoff is **n_q=5, A-independent** (vs heuristic growing to 11 at A=100) — the nb/N_f study confirms n_b=4–5 safe. Threaded through the sweep dispatcher and forwarded into the NS-amplitude register (n_b=n_q+1). Test: `tests/test_tong_cutoff.py`. The amplitude 'energy_bound' (Lemma 5) path ignores it. Full Theorem-6 prefactor still open homework (CLAUDE.md).
  - EN-PT2 pass-1 (the `connections` sweep + diagonal H_aa) ported to C++ (`MixedProvider.pt2_accumulate` in `backend_fork/`, via `backend.cpp_pt2_external`), auto-detected by `epstein_nesbet_pt2(use_cpp=None)` with the pure-Python path kept as fallback + reference. **17× (229 terms) → 47× (1265) → 57× (4897, L=3 d=3)** on pass-1, bit-identical dE_PT2 (≤1e-13) — unblocks PT2 at L≥4 / larger cores. Test: `classical/trimci/backend_fork/test_pt2_cpp.py`.
- **Extensivity-trap mitigation, Phase A + B (2026-07-09).** Classical (TrimCI) side, groundwork for the headline dets-vs-L exponent. (A) Size-intensive accuracy is now first-class: `extrapolation.report_energies(sites=...)` emits the per-site mirror of every headline number, and `run_cpp.l_scaling_sweep(gate=...)` switches the convergence gate between size-extensive-normalized relative (`|dE|/|E|`) and size-intensive per-site (`|dE|/sites` MeV/site) — both drops always recorded. (B) `run_cpp.converged_reference(L)` builds a per-L reference `E_inf(L)±σ` (independent core ladder → EN-PT2 → SHCI/PT2 + power-law extrapolation) and decides, per accuracy target eps, whether E_inf is pinned tightly enough to define a fixed-eps N* (a "point": validated vs exact, or σ/site and extrapolator-agreement both < fractions of eps) or only a "bound". Validated: L=2 d=1 pins as a point vs Lanczos ((extrap−exact)/site=−0.0007 MeV/site); L=2 d=3 from small cores correctly reports BOUND (extrapolators disagree 1.36 MeV/site). CLI `--mode reference`. Tests [35][36]; details in `classical/trimci/TODO.md` (2026-07-09).
- **Extensivity-trap Phase C (2026-07-09) — the dets-vs-L exponent.** `run_cpp.dets_vs_L_at_fixed_accuracy` measures N*(L; eps) = determinants to reach a fixed per-site accuracy eps of the per-L reference, then contrasts an exponential-in-volume vs polynomial-in-volume fit (γ = the paper's central number). Honest by construction: `_extract_nstar` brackets N* on a stability criterion (guards a bursty fluke pass), reference σ is propagated into N*, and a point counts only if the reference is pinned AND N* is bracketed — else it's a logged bound (▲/▼), never a silent drop. Adaptive wall-clock-bounded ladder; plot `plot_dets_vs_L` (log-lin + log-log panels); driver `misc/run_dets_vs_L.py`; test [37]. **Laptop L=2–4 dilute run (dim=3, cores→16k, 59.5 min): every (L,eps) is a LOWER BOUND — nothing pins** (E_var+PT2 still 3.4–4.7 MeV/site from E_inf at the top core; extrapolators disagree ~1–12 MeV/site) ⇒ the trustworthy exponent needs HPC (quantified). Salvageable size-intensive result: at fixed core=4000 the per-site PT2 correction grows −2.09→−6.50→−9.51 MeV/site for L=2→3→4 — the extensivity trap made visible. Fixed-filling companion + HPC push are next. See `classical/trimci/TODO.md` (2026-07-09 Phase C).
- **Extensivity-trap Phase D (2026-07-10) — honesty & robustness guards.** New `classical/trimci/robustness.py` (pure): `ladder_monotonicity` (flags a bursty energy rise as core grows), `pt2_memory_report` (tracks EN-PT2 external-space `n_ext`, fires a semistochastic-PT2 trigger past a budget — deterministic PT2 sums the full connected space exactly, which doesn't scale forever), `scatter_stats`; wired into `converged_reference` / `dets_vs_L` outputs, plus an auto-computed extensivity signal (dE_PT2/site vs L at a fixed core). New `run_cpp.seed_robustness` (CLI `--mode seed_robustness`) re-solves the same (L,core) across independent base seeds (no warm starts) to measure ensemble scatter. **KEY FINDING: the n_runs=4 ensemble is SEED-FRAGILE at laptop cores** — L=2 d=3 core=2000 spans 6.8 MeV/site (54 MeV) across seeds, while the single-seed ladders all look monotone — so Phase-C rungs carry ~7 MeV/site seed uncertainty on top of the extrapolation uncertainty (compounds the "needs HPC / more ensemble runs" verdict). The reference-is-extrapolated circularity + the L=2 d=1-vs-exact standing validation are documented in the module docstring. Test [38]. Stretch (flagged, not built): a DMRG/`block2` independent cross-check at one 3D point. See `classical/trimci/TODO.md` (2026-07-10 Phase D).
- **Classical-baseline rule-out campaign, lit-review phase done (2026-07-10).** New `claude/research/classical_baselines/` (gitignored) with a top-level `00_overview.md` synthesis + four sub-reviews (`afqmc_sign_problem/`, `tensor_networks/`, `coupled_cluster/`, `other_methods/`), ~49 PDFs total. Goal: establish "selected CI (TrimCI) is the least-bad classical option for this model." **Verdicts:** AFQMC/DQMC **ruled out** — sign-problematic *even at A=Z* (the σ⊗τ off-diagonal transition vertex breaks the det_↑·det_↓ factorization that makes Holstein/SSH sign-free; WT is imaginary-antisymmetric; mitigations trade the variance wall for uncontrolled bias). DMRG/MPS **ruled out** in 3D (χ~q^(L²); boson local dim 16·N_f³ compounds; wall between L=2 feasible and L=3–4 impossible), **niche** as an L=2³ cross-check. Coupled cluster **niche/weak-coupling only** (single-ref CC → non-real energies in the strong-attractive-contact limit; accurate only where sCI is trivially cheap). **NQS is the one live threat (HIGH)** — closest structural match (Mahajan–Robinson–Lee–Reichman 2024 e-ph NQS) but nobody has built nuclear-fermion ⊗ pion-Fock NQS, and it degrades in the strong-SSH regime H_AV sits in; must be tested (could flip the baseline). Demo campaign (proposals only, not yet built): **D-CC** (pyscf CCSD(T) via fermionized FCIDUMP, coupling-scale sweep), **D-AFQMC-sign** (⟨s⟩ vs β histogram), **D-DMRG** (block2 ladder L=2→4, P3=2³ doubles as the independent 3D cross-check from Phase D), **D-NQS** (NetKet, the baseline-flip test). See `00_overview.md` for the verdict table + full campaign.
- **Tier-1 rule-out demos built (2026-07-10).** New `classical/baselines/` module + tests, implementing the two cheapest campaign items (details: `claude/research/classical_baselines/01_tier1_results.md`). Uses **pyscf** (installed into `.venv` via `VIRTUAL_ENV=.venv uv pip install pyscf` — Claude-side tooling, deliberately kept out of `requirements.txt`). (1) **D-CC** (`cc_reduction.py`, `tests/test_cc_reduction.py`): CCSD/CCSD(T) on the CC-friendliest fermionic reduction (pion frozen out → a 2-body spin-independent 4-component Hubbard-like H; validated pyscf-FCI==OpenFermion to <1e-6, and excitation spectrum == the project's own static-nucleon sector). At physical contact `s=1` the RHF reference is internally unstable, RHF-CCSD fails to converge (d=3) and is non-variational (E_CCSD < E_FCI by 58 MeV at s=2), UHF-CCSD diverges; FCI (the selected-CI target) exact throughout → CC ruled out except at trivially-weak coupling. (2) **D-AFQMC-sign** (`sign_structure.py`, `tests/test_sign_structure.py`): exact term-resolved sign diagnostics on the full mixed H. **H_WT is exactly the source of a phase problem** — 16% of off-diagonal elements complex, EXACTLY 0% with H_WT off (test-asserted), robust 11–16% across sizes; **H_AV (Yukawa) drives the worldline Δ_sign** (>0, extensive in L: /site 0.0129→0.0148 for L=2→3), ⟨s⟩(β) decays exponentially. Honest wrinkle: A=4 (L=2 1D) is an accidentally sign-free filling (Δ_sign≈0, phase fraction still 13%) — mirrors the SU(4)-protected-point story. Both confirm the lit-review verdicts. Remaining campaign items (not built): D-DMRG (block2, doubles as 3D cross-check) and D-NQS (the baseline-flip test).
- **D-DMRG built + a load-bearing cross-check finding (2026-07-10).** `classical/baselines/dmrg_entanglement.py` (exact bond-dim lower bound) + `dmrg_block2.py` (actual DMRG via **block2**, installed `VIRTUAL_ENV=.venv uv pip install block2`), `tests/test_dmrg.py` (2/2). Details: `claude/research/classical_baselines/02_dmrg_results.md`. The block2 adapter is a custom mixed fermion-boson **complex** MPO (site→4 fermion + 3 pion modes; `U1Fermi` symmetry, `SAny|CPX`; unique scratch under `data/.block2_scratch/`), **validated vs Lanczos to 7.8e-12 (L=2 1D), 1.2e-3 (L=3 1D)**. DMRG wall confirmed: χ needed ~doubles per added dimension, wall-time 2.7→10.5→94 s for L=2 1D→2D→3D; exact entanglement χ*(1e-6)=43→252 for a 1D→2D cut, flat in 1D. **KEY FINDING — the L=2³ cross-check (out of exact Lanczos reach) exposed a real TrimCI ensemble problem:** block2 DMRG (E=2246.1, converged) vs TrimCI selected CI — with the DEFAULT small ensemble (n_runs≤6) TrimCI is **basin-trapped at 2402 MeV (~20 MeV/site high) and does NOT improve from 4k→30k dets**; raising to **n_runs≥16** escapes to 2268.5 (2.8 MeV/site from block2, seed-robust). So the cross-check both *validates* TrimCI at 3D AND says **use n_runs≥16 for the HPC runs** (ensemble breadth > raw det count here; compounds the Phase-D seed-fragility note). DMRG verdict stands: ruled out for production L≥3 (χ~e^{L²} wall), niche as the small-L cross-check — which just earned its keep. Remaining campaign item: D-NQS (the baseline-flip test).
- **D-NQS built — the baseline-flip test; it does NOT flip (2026-07-10).** `classical/baselines/nqs_netket.py` + `tests/test_nqs.py` (2/2). Details: `claude/research/classical_baselines/03_nqs_results.md`. Uses **NetKet 3.17** (`VIRTUAL_ENV=.venv uv pip install netket`). Our exact mixed H is wired into NetKet as a custom `DiscreteOperator` wrapping the project's `connections` oracle (same H as block2/TrimCI/Lanczos) — **validated: NetKet exact-diag == Lanczos to 3.4e-13**. Findings: (1) off-the-shelf NQS (RBM α=2/4/8, RBMModPhase, MLP; FullSum exact-expectation VMC + SR) **stalls ~11 MeV (2.8–5.5 MeV/site) above the ground state at L=2 1D** — more params don't help, α=16 unstable — while TrimCI matches Lanczos to 0.017 MeV there. (2) A controlled toggle shows the gap **persists with pion coupling off**, so it's NOT the σ⊗τ sign structure (unlike AFQMC/CC) — it's plain mixed-Fock representation/optimization difficulty. (3) At L=2 3D the instantiation is blocked by sampler engineering (`MetropolisLocal` breaks A-conservation → samples ~16 fermions not 2; `HamiltonianRule` needs a JAX-native oracle, hangs with our Python one). **Verdict: NQS doesn't beat selected CI today; a competitive instantiation (backflow/Jastrow + JAX-native oracle + constrained sampler) is the unbuilt engineering barrier the survey named.** THE CLASSICAL-BASELINE RULE-OUT CAMPAIGN IS COMPLETE — all four families (CC, QMC-sign, DMRG, NQS) ruled out with runnable evidence; selected CI (TrimCI) confirmed least-bad, provided it's run with n_runs≥16 (per the D-DMRG cross-check).
- Current uncommitted changes — verify with `git diff` before assuming.
