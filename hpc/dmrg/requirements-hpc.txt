# Runtime for the block2-DMRG isospectrality-reference HPC shards (Python 3.10).
# build_from_eft needs openfermion (pulls cirq); DMRG needs block2 (Linux manylinux
# wheel, installs clean via uv). The mixed_ci C++ extension is NOT needed here --
# classical.trimci.backend imports it lazily (try/except), and DMRG uses block2's own
# MPO, not the TrimCI C++ solver -- so there is no compile step in this workflow.
numpy==1.26.4
scipy==1.12.0
openfermion==1.6.1
cirq==1.4.0
block2==0.5.3
# block2 0.5.3 pins mkl==2021.4 (uv would pull it anyway; stated here for clarity). Its
# wheel vendors libmkl_core/rt into block2.libs/ but STRIPS the dynamically-loaded MKL
# kernel libs (libmkl_def/avx/vml...), and MKL loads those from the dir where libmkl_core
# lives -> the run script copies this wheel's kernel libs into block2.libs/ so it works.
mkl==2021.4
